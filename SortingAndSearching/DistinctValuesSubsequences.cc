#include <bits/stdc++.h>
using namespace std;

int main() {
    long n;
    cin >> n;
    vector<long> input(n);
    for (int i = 0; i < n; i++)
        cin >> input[i];
    cout << 0 << endl;
    return 0;
}
